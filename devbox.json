{
  "$schema": "https://raw.githubusercontent.com/jetify-com/devbox/0.13.7/.schema/devbox.schema.json",
  "packages": [
    "python@3.12",
    "uv@latest"
  ],
  "env": {
    "LD_LIBRARY_PATH": "$DEVBOX_PACKAGES_DIR/lib:${LD_LIBRARY_PATH:-}",
    "UV_PROJECT_ENVIRONMENT": ".venv"
  },
  "shell": {
    "init_hook": [
      "[ -d .venv ] || uv venv --python python3.12",
      "source .venv/bin/activate",
      "uv sync --dev 2>/dev/null || echo 'hint: run uv sync --dev to install deps'"
    ],
    "scripts": {
      "test":   "uv run pytest -m 'not slow and not integration'",
      "test-all": "uv run pytest",
      "lint":   "uv run ruff check . && uv run mypy ccmcp",
      "fmt":    "uv run ruff format .",
      "serve":  "uv run python -m ccmcp serve",
      "start":  "uv run python -m ccmcp start",
      "ingest": "uv run python -m ccmcp ingest"
    }
  }
}
